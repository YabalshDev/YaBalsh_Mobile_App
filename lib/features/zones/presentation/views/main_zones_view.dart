import 'package:flutter/material.dart';

import '../widgets/main_zones_body.dart';

class MainZonesView extends StatelessWidget {
  const MainZonesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: MainZonesBody());
  }
}
